<div>
<div class="card" style="width: 50rem;margin:auto;">
  <img src={{picFinal}} id="img">
  <div class="card-body">
    <h5 class="card-title">
        <ul>{{pet.name}}</ul></h5>
    <p class="card-text">

    
    <li> Size: {{pet.size}}</li>
    <li> Age: {{pet.age}}</li>
    <li> Breed: {{pet.breed}}</li>
    <li> Location: {{pet.location}}</li>
      <p>
        Description: 
        <ul>{{pet.description}}</ul>
    </p>
    <h5>Current Owner: {{pet.user.name}}</h5>
    <h5>Email: {{pet.user.email}}</h5>
    <h5>Phone Number: {{pet.user.phone_number}}</h5>
   </div>
</div>
</div> 


